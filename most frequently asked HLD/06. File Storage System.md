# System Design: File Storage System (Beginner-Friendly Guide)

---

## What Are We Building?

Think of Amazon S3 or Google Cloud Storage — a massively scalable, reliable object storage service where users can:
- Upload files of any size (KB to TB)
- Download files from anywhere globally
- Organize files into "buckets"
- Access files programmatically via API
- Version files (keep history)
- Organize files with lifecycle policies (auto-delete old versions)
- Control who can access files

The interesting engineering challenges hidden in this simple flow:
- **Scale** — exabytes (1 million terabytes) of data across millions of users; can't store on single machine
- **Durability** — "11 nines" (99.999999999% uptime) means at most 31 seconds of data loss per year across the entire planet
- **Latency** — user in Tokyo should download file as fast as user in New York (requires global CDN)
- **Cost optimization** — storing old data on expensive SSDs wastes billions; move to cheaper storage (Glacier) automatically
- **Replication** — if data center burns down, data must exist in another country
- **Consistency** — user uploads file, should be readable immediately by anyone who knows the URL
- **Metadata** — index millions of files per bucket so users can search/list quickly

---

## Step 1: Design Scope

**Scale:**
| Parameter | Value |
|-----------|-------|
| Total data stored | 1+ exabytes (1 billion TB) |
| Concurrent uploads | 1 million |
| Concurrent downloads | 10 million |
| Upload throughput | 100 petabytes/day (avg) |
| Object size range | 1 byte - 5 TB |
| Average object size | 100 MB |
| API requests/sec (peak) | 1 million |
| Metadata records | 100+ billion objects |

**Key feature:** Data durability is paramount. Losing user data is unacceptable. Even if server fails, data must be recoverable.

**QPS Funnel:**
```
PUT (upload) requests:     QPS = 100,000/sec
GET (download) requests:   QPS = 500,000/sec
LIST requests:             QPS = 50,000/sec
DELETE requests:           QPS = 10,000/sec
```

**Non-functional requirements:**
- Durability: 99.999999999% (11 nines)
- Availability: 99.99% uptime
- Upload latency: < 100ms to confirm receipt
- Download latency: < 100ms to start download
- Support for global access (low latency everywhere)
- Cost-effective storage (cheap for old data)

---

## Step 2: API Design

**Bucket Operations:**
```
POST   /buckets                        ← create bucket
DELETE /buckets/{name}                 ← delete bucket
LIST   /buckets                        ← list user's buckets
```

**Object Operations:**
```
PUT    /buckets/{name}/objects/{key}   ← upload object
GET    /buckets/{name}/objects/{key}   ← download object
DELETE /buckets/{name}/objects/{key}   ← delete object
LIST   /buckets/{name}/objects         ← list objects
HEAD   /buckets/{name}/objects/{key}   ← get metadata only
```

**Example: Upload Object**
```json
PUT /buckets/my-bucket/objects/users/profile.jpg
Content-Type: image/jpeg
Content-Length: 5242880

[binary image data]

Response:
{
  "bucket": "my-bucket",
  "key": "users/profile.jpg",
  "version_id": "v123456",
  "etag": "5d41402abc4b2a76b9719d911017c592",
  "size": 5242880,
  "created_at": "2026-06-18T10:35:00Z"
}
```

**Example: Multipart Upload (for large files > 5GB)**
```
1. Initiate:
   POST /buckets/my-bucket/objects/large-video.mp4?uploads
   Response: {"uploadId": "abc123"}

2. Upload Parts (parallel):
   PUT /buckets/my-bucket/objects/large-video.mp4?uploadId=abc123&partNumber=1
   PUT /buckets/my-bucket/objects/large-video.mp4?uploadId=abc123&partNumber=2
   ...

3. Complete:
   POST /buckets/my-bucket/objects/large-video.mp4?uploadId=abc123
   Body: [list of part ETags]
   Response: Final object metadata
```

---

## Step 3: Database Design — Why Hybrid?

| Requirement | Solution |
|-------------|----------|
| Object metadata | SQL database (PostgreSQL) — queryable, structured |
| Versioning & history | SQL + time-series DB — can query by date |
| Geographic replication | Distributed SQL — sync across regions |
| Bucket list/search | Elasticsearch — fast full-text search |
| Large object data | Distributed file system — HDFS, custom |

---

## Step 4: Data Storage Layers

### Hot Tier (Recent, Frequently Accessed)
```
Storage: SSD in 3 data centers
Cost: $0.023/GB/month
Speed: < 100ms latency
Use case: Downloads by active users
```

### Warm Tier (Medium Access)
```
Storage: HDD in 2 data centers
Cost: $0.0125/GB/month
Speed: 500ms latency
Use case: Occasional downloads, older files
```

### Cold Tier (Archive, Rarely Accessed)
```
Storage: Glacier (offline tape)
Cost: $0.004/GB/month
Speed: > 1 hour to retrieve
Use case: Compliance archives, backups
Automatic transition: Move warm → cold after 90 days
```

---

## Step 5: Replication & Durability

### 3-Way Replication
```
Original data → Replica in different rack
              → Replica in different region

Ensures:
- Survive single data center failure
- Survive regional disaster
- Durability: 99.999999999%
Cost: 3x storage
```

### Erasure Coding (More Efficient)
```
Data split: D1, D2, D3, D4 (4 blocks)
Parity added: P1, P2 (2 blocks)
Total stored: 6 blocks

Recover if 2 blocks lost (need 4 of 6)
Cost: 1.5x storage (6/4)
vs 3x with replication
```

---

## Step 6: High-Level Architecture

```
User (Upload/Download)
       ↓
[CDN] ← Edge servers globally
       ↓
[API Gateway] (auth, route)
       ↓
[Upload Service] [Download Service]
       ↓
[Storage Cluster] ← Data replicated across regions
       ↓
[Metadata DB] ← PostgreSQL
       ↓
[Indexing] ← Elasticsearch (for search)
```

**Components:**
- **API Gateway:** Handles auth, rate limiting, request routing
- **Upload Service:** Handles multipart uploads, parallel chunks
- **Storage Cluster:** Distributed storage with replication/erasure coding
- **Metadata DB:** Tracks object versions, ownership, ACLs
- **CDN:** Global distribution for fast downloads
- **Backup Service:** Cross-region replication, disaster recovery

---

## Step 7-13: Deep Dives Already Covered

[File Storage implementation details covered in original content: multipart uploads, versioning, lifecycle policies, access control, etc.]

---

## Interview Cheat Sheet Q&A

**Q: Why multipart upload when single upload is simpler?**  
A: Single upload fails on large files or unstable networks. Multipart: split 1GB into 10 × 100MB chunks. If chunk 5 fails, retry only chunk 5 (not all 10). Upload chunks in parallel: 8x faster. Also, metadata server tracks progress: "9 of 10 chunks received."

**Q: Why 3-way replication instead of erasure coding?**  
A: Replication is simpler. Erasure coding saves space (1.5x vs 3x) but slower writes (must calculate parity) and slower rebuilds (more complex). S3 uses replication for regular files, erasure coding for archival tiers.

**Q: How to handle if user's region data center burns down?**  
A: Data already replicated to other regions. API detects region down, routes to secondary region. Apps shouldn't notice (maybe 1-2 seconds delay). RTO (recovery time) < 1 minute. RPO (data loss) = 0.

**Q: Can we compress files to save cost?**  
A: Yes, but transparent compression is risky. If compression algorithm has bug, file corrupts on decompression. Better: recommend users compress before upload. S3 lets admins set storage policies: "Auto-compress PDF files" with caveats.

**Q: What if a file is accessed 1 million times in 1 day?**  
A: Without CDN, 1M downloads all hit main data center, network saturates. With CDN: file cached at 1000+ edge locations worldwide. User in Tokyo downloads from Tokyo edge (cached locally). Reduces bandwidth by 100x.

**Q: How to prevent someone from accessing another user's file?**  
A: ACL on each object. Default: private (only owner). Owner can make public or share via presigned URL (time-limited, signature).

---

## Full Flow (Start to End)

### Happy Path
1. Client request enters API Gateway and is authenticated/authorized.
2. Orchestrator service validates input and routing context.
3. Core service executes primary business logic and required checks.
4. Read path uses cache first; fallback goes to durable database/store.
5. Write path updates fast layer first (where applicable) and publishes async events.
6. Downstream consumers persist durable state and trigger secondary effects.
7. Response is returned to client with final status and metadata.

### Failure and Retry Paths
1. Cache miss: read from durable store, then repopulate cache.
2. Dependency timeout: retry with backoff or circuit-breaker fallback.
3. Async event failure: retry queue and dead-letter queue (DLQ) handling.
4. Duplicate request: idempotency key returns prior successful outcome.
5. Concurrent updates: version/lock conflict triggers re-read and safe retry.

### End-State Guarantees
- Low-latency user operations on the hot path.
- Durable correctness in the source-of-truth datastore.
- Eventual consistency for non-critical async side effects.
- Strict correctness at critical boundaries (commit/payment/finalization).

---
## Summary

A scalable file storage system requires:
- ✅ Distributed storage with replication/erasure coding
- ✅ Tiered storage (hot/warm/cold) for cost optimization
- ✅ Multipart upload for large files
- ✅ Global CDN for fast downloads
- ✅ Scalable metadata management
- ✅ Versioning and lifecycle policies
- ✅ Access control and security
- ✅ Disaster recovery and geo-replication


---

## What Are We Building?

A scalable object storage service like Amazon S3 that:
- Stores and retrieves large files reliably
- Supports multiple storage tiers
- Ensures high durability and availability
- Handles global distribution
- Manages versioning and metadata
- Provides access control

**Key Challenges:**
- Petabyte-scale data storage
- High availability (99.99%)
- Low latency access globally
- Cost optimization
- Durability (11 nines - 99.999999999%)
- Scalable metadata management

---

## Step 1: Requirements

### Functional Requirements
- Upload/download files
- Delete files
- List objects in bucket
- Manage buckets
- Versioning support
- Access control (ACL, IAM policies)
- Metadata management
- Search by filename/tags
- Lifecycle policies (auto-deletion)

### Non-Functional Requirements
- Durability: 99.999999999% (11 9s)
- Availability: 99.99% uptime
- Latency: Sub-100ms read
- Scalability: Exabytes of data
- Cost-effective storage
- Global accessibility
- Security and encryption

---

## Step 2: Architecture Overview

```
┌──────────────────────┐
│   Client Apps        │ (Web, Mobile, API)
└──────┬───────────────┘
       │
   ┌───▼──────────────────────┐
   │  API Gateway             │
   │  (Auth, Rate Limit)      │
   └───┬──────────┬───────────┘
       │          │
   ┌───▼──┐   ┌───▼──────────────┐
   │Upload│   │Metadata Service  │
   │Engine│   │(Bucket, Object)  │
   └───┬──┘   └────┬─────────────┘
       │           │
   ┌───┴───────────┴─────────────┐
   │                             │
┌──▼──────────────┐   ┌──────────▼──────┐
│ Storage Nodes   │   │ Database Layer  │
│ (Distributed)   │   │ (Metadata, etc) │
└────────────────┘   └─────────────────┘
```

---

## Step 3: Key Components

### 1. API Gateway
- Handles upload/download requests
- Authentication and authorization
- Rate limiting
- Request routing

### 2. Upload Service
- Multipart upload support
- Chunked transfer
- Resumable uploads
- Client-side encryption support

### 3. Metadata Service
- Stores bucket and object metadata
- Manages object versions
- Handles access control lists
- Search indexing

### 4. Storage Nodes
- Distributed storage
- Data replication
- Erasure coding
- Data integrity checks

### 5. Retrieval Service
- Object lookup
- Data reconstruction
- Caching frequently accessed objects

---

## Step 4: Data Organization

### Bucket Structure
```
Bucket: unique name across all accounts
├── Object: unique within bucket
│   ├── Key: object path (e.g., users/profile.jpg)
│   ├── Value: actual data
│   ├── Metadata:
│   │   ├── Size
│   │   ├── Content-Type
│   │   ├── Created-At
│   │   ├── Last-Modified
│   │   ├── ETag (hash)
│   │   ├── Version-ID
│   │   ├── Owner
│   │   └── Tags
│   └── ACL: Who can access
```

### Bucket Naming
```
Global namespace - must be unique across all users
Rules:
- 3-63 characters
- Lowercase, numbers, hyphens
- Cannot start/end with hyphen
- Cannot be IP address format

Example:
- my-company-backups
- project-artifacts-2024
- logs-prod-us-east
```

---

## Step 5: Upload Flow - Multipart Upload

### Simple Upload (< 5GB)
```
PUT /bucket/key
Content: File data
Response: ETag, Version ID
```

### Multipart Upload (> 5GB)

```
1. Initiate:
   POST /bucket/key?uploads
   Response: UploadID

2. Upload Parts (parallel):
   PUT /bucket/key?uploadId=abc&partNumber=1
   PUT /bucket/key?uploadId=abc&partNumber=2
   PUT /bucket/key?uploadId=abc&partNumber=3
   Response: ETags for each part

3. Complete Upload:
   POST /bucket/key?uploadId=abc
   Body: List of part ETags
   Response: Final ETag, Version ID

Advantages:
- Parallel uploads (faster)
- Resumable (can restart failed parts)
- Better error handling
```

### Upload Optimization
```
Parallel Uploads:
- Split file into chunks (5-100MB each)
- Upload 4-8 chunks concurrently
- Combine parts server-side
- Result: 4-8x faster upload

Resume Capability:
- Client tracks uploaded parts
- On failure, retry failed parts
- Don't re-upload successful parts
- Save bandwidth for large files
```

---

## Step 6: Data Durability - Replication & Erasure Coding

### Replication Strategy
```
3-Way Replication (simpler but expensive):
Original → Replica 1 (different rack)
        → Replica 2 (different region)

Write to master, replicate to followers
Durability: 99.999999999% (11 9s)

Cost: 3x storage
```

### Erasure Coding (Reed-Solomon)

```
More efficient than replication
Data + Parity blocks

Example (4+2):
Data:   D1  D2  D3  D4
Parity: P1  P2

Total 6 blocks stored
Recover original if any 4 blocks available
Lose 2 blocks, still recover

Cost: 1.5x storage (6/4)
vs 3x with replication

Trade-off: Slower writes (need parity calc)
         Faster reads (can use any 4 blocks)
```

### Hybrid Approach
```
Hot tier: Recent, frequently accessed
→ 3-way replication (fast access)

Warm tier: Medium access frequency
→ Erasure coding (balanced cost/speed)

Cold tier: Archive, rarely accessed
→ Erasure coding with geographically dispersed

Auto-transition based on access patterns
```

---

## Step 7: Download/Retrieval

### Efficient Download

```
1. Client requests object:
   GET /bucket/key

2. API Gateway queries metadata:
   - Get object location
   - Check access permissions
   - Get object version

3. Retrieve from nearest storage node:
   - Use CDN for frequently accessed
   - Parallel download from multiple nodes
   - Reconstruct if using erasure coding

4. Return to client with metadata:
   - Content-Type
   - Content-Length
   - Last-Modified
   - ETag
   - Cache-Control headers
```

### Range Requests
```
GET /bucket/key
Range: bytes=1000-1999

Use case:
- Video streaming (fetch segments)
- Large file download (resume)
- Resume interrupted transfer
```

---

## Step 8: Versioning

### Object Versioning

```
Enable versioning on bucket:
PUT /bucket/key (v1)
PUT /bucket/key (v2)
PUT /bucket/key (v3)

Each version has unique version ID
Can retrieve any version
Delete doesn't remove old versions

Database tracks:
- Object Key
- Version ID
- Content
- Timestamp
- Delete marker (if deleted)
```

### Delete Behavior
```
Without versioning:
DELETE /bucket/key → Permanently deleted

With versioning:
DELETE /bucket/key → Adds delete marker
                      Old versions still exist
                      Can restore by removing marker
```

---

## Step 9: Lifecycle Policies

### Auto-Management of Objects

```
Rule 1: Archive after 90 days
- 0-30 days: Standard storage (hot)
- 30-90 days: Infrequent Access (warm)
- 90+ days: Glacier (cold/archived)

Rule 2: Delete old versions
- Keep current version forever
- Delete previous versions after 30 days
- Saves storage costs

Rule 3: Delete incomplete uploads
- Remove multipart uploads > 7 days old
- Cleanup abandoned uploads
```

### Storage Tiers
```
Standard: $0.023/GB/month (hot data)
IA (Infrequent Access): $0.0125/GB/month (warm data)
Glacier: $0.004/GB/month (cold/archive)
Deep Archive: $0.00099/GB/month (rarely accessed)

Cost optimization:
Old data automatically moves to cheaper tier
Saves 80% on storage for old data
```

---

## Step 10: Metadata Management

### Metadata Database Schema

```
Objects Table:
├── bucket_name (partition key)
├── object_key (sort key)
├── version_id
├── size
├── content_type
├── created_at
├── last_modified
├── etag
├── owner_id
├── storage_class
├── encryption
├── metadata_tags (JSON)
└── acl (JSON)

Buckets Table:
├── bucket_name (primary key)
├── owner_id
├── region
├── created_at
├── versioning_enabled
├── encryption_enabled
├── public
└── lifecycle_rules (JSON)
```

### Metadata Indexing
```
Primary Index: (bucket_name, object_key, version_id)
Secondary Indexes:
- By owner: owner_id
- By timestamp: created_at
- By size: size (for analytics)
- By tag: metadata_tags
```

---

## Step 11: Access Control

### Bucket Policies

```json
{
  "Version": "2024-06-18",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::my-bucket/*",
      "Condition": {
        "IpAddress": {
          "aws:SourceIp": "192.168.1.0/24"
        }
      }
    }
  ]
}
```

### Presigned URLs
```
Generate time-limited access URL:
https://bucket.s3.amazonaws.com/key?
  Signature=...&
  Expires=1624050000&
  AWSAccessKeyId=...

Use cases:
- Share file without public bucket
- Temporary download links
- Form uploads without secret key
```

---

## Step 12: Scalability & Performance

### Horizontal Scaling

**Storage Nodes:**
- Distributed across multiple data centers
- Consistent hashing for object placement
- Parallel read/write

**Metadata Service:**
- Partition by bucket name
- Replicate across regions
- Cache frequently accessed metadata

**Load Distribution:**
- Request router (based on bucket, geographic location)
- Auto-scaling API gateways
- CDN for popular objects

### Performance Optimization

```
1. Request Batching:
   - List 1000 objects per request
   - Multi-part uploads in parallel

2. Caching:
   - Cache metadata in memory
   - Cache frequently accessed objects in edge locations
   - Cache prefixes for list operations

3. Connection Pooling:
   - Reuse TCP connections
   - Reduce latency
   - Improve throughput
```

---

## Step 13: Disaster Recovery

### Backup Strategy
```
Cross-region replication:
- Primary region: All read/write
- Secondary region: Read-only replica
- RTO: < 1 hour
- RPO: < 5 minutes

Backup frequency:
- Hourly snapshots of metadata
- Real-time replication of data
```

### Failure Scenarios
```
Node failure:
→ Rebuild from replicas/erasure coding
→ 1-2 hours recovery

Region failure:
→ Route to secondary region
→ Fail over within minutes

Metadata loss:
→ Reconstruct from data blocks
→ Use backup metadata DB
```

---

## Step 13: Key Design Decisions & Tradeoffs

| Decision | Why? | Tradeoff |
|----------|------|----------|
| Multipart upload | Resume from failure; parallel uploads = 4-8x faster | More complex client logic; server-side state management |
| Erasure coding over replication | 1.5x storage vs 3x; cheaper | Slower writes (parity calculation); complex recovery |
| S3-style bucket organization | Universally familiar; good namespace isolation | Global uniqueness constraint; no nested buckets |
| Tiered storage (hot/warm/cold) | Cost savings (older data cheaper storage) | Complexity; slower access to cold tier; cross-tier movement |
| Eventual consistency | Higher availability; no coordination overhead | Brief stale reads; hard to debug |

## Interview Cheat Sheet Q&A

**Q: Why allow multipart upload when single-request upload is simpler?**  
A: Single upload fails on large files (network timeout, server restart). With multipart, if chunk 3 of 10 fails, retry only chunk 3, not all 10. Also, upload chunks in parallel: chunk 1 on connection 1, chunk 2 on connection 2, etc. 4 parallel = ~4x faster.

**Q: Why use erasure coding instead of simple 3-way replication?**  
A: Replication wastes space. Need 3 copies for durability. Erasure coding (e.g., 4+2): split file into 4 data blocks + 2 parity blocks. Need only 4 of 6 to recover. Storage = 1.5x, not 3x. Over millions of files, this saves billions of dollars. Tradeoff: writes need parity calculation (slower), but writes are rare compared to reads.

**Q: What if a user deletes a file, then wants it back in 3 months?**  
A: Without versioning, it's gone forever. With versioning, old versions are retained (and charged). User can restore. Tradeoff: costs more (storing multiple versions). Best practice: keep versions for 30 days for free, charge per version after that.

**Q: Can we serve files directly from S3 without a CDN?**  
A: Technically yes. User downloads directly from S3. Works fine for a few hundred users. But millions of concurrent downloads hit rate limits and need DDoS protection. CDN has thousands of edge servers, so requests are distributed, and popular files are cached locally at CDN edges.

**Q: How do we prevent someone from sharing a private file's URL?**  
A: Use presigned URLs that expire. "GET /file?signature=xyz&expires=1624050000". After expiry, URL is invalid even if shared. Also, use authentication: only the file owner can generate presigned URLs.

**Q: What if metadata database (file listing) is corrupted?**  
A: Data is still safe in storage, just orphaned. Rebuild metadata by scanning storage: "What data blocks exist?" → infer original files. Takes time but recovers data. This is why immutable infrastructure is important.

 Summary

A scalable file storage system requires:
- ✅ Efficient multipart upload mechanism
- ✅ Durability through replication/erasure coding
- ✅ Scalable metadata management
- ✅ Tiered storage for cost optimization
- ✅ Versioning and lifecycle policies
- ✅ Access control and security
- ✅ Global distribution and CDN
- ✅ Disaster recovery and backup



