# System Design: Ride-Sharing Backend (Beginner-Friendly Guide)

---

## What Are We Building?

A backend for an Uber-like ride-sharing platform where:
- Users request a ride (driver picks them up)
- Drivers see requests and accept rides
- System matches nearby available drivers to riders
- Real-time location tracking for both driver and rider
- ETA calculation (pickup time, trip duration)
- Payment processing
- Ratings and reviews

**Key Engineering Challenges:**
- **Real-time matching** � Rider requests at 8pm; system has 2 seconds to find available driver and notify them
- **Geospatial queries** � Find all drivers within 5km radius of rider; avoid scanning millions of drivers
- **Scale** � 1 million concurrent riders + drivers; each driver updates location every 10 seconds = 100K location updates/sec
- **Latency** � Matching takes too long? User cancels. Target: < 200ms
- **Consistency** � Driver offline, but system thinks they're available; assign them a ride they can't reach
- **Surge pricing** � When demand > supply, pricing increases to attract drivers and reduce demand
- **ETA accuracy** � If ETA is wrong, rider waits too long or driver is stuck in traffic

---

## Step 1: Design Scope

**Scale:**
| Parameter | Value |
|-----------|-------|
| Daily active riders | 10 million |
| Daily active drivers | 2 million |
| Concurrent riders | 1 million (8pm rush) |
| Concurrent drivers | 500K available |
| Peak QPS | 100K requests/sec |
| Location updates/sec | 100K |
| Matching latency target | < 200ms |
| Driver acceptance rate | 70% |
| Cancellation rate | 5% |

**QPS Funnel:**
```
Ride requests:         50,000 QPS
Location updates:     100,000 QPS
Matching operations:   50,000 QPS
ETA calculations:      50,000 QPS
Payment transactions:  10,000 QPS
```

**Non-functional requirements:**
- Availability: 99.99% uptime
- Latency: Matching < 200ms, location update < 100ms
- Consistency: Driver location stale < 30 seconds
- Scalability: Handle 2x peak traffic (from surge events)
- Security: Payment info encrypted, user privacy protected

---

## Step 2: API Design

**Rider APIs:**
```
POST   /rides/request           ? Request a ride
GET    /rides/{rideId}          ? Get ride status
PATCH  /rides/{rideId}/cancel   ? Cancel ride
POST   /rides/{rideId}/rate     ? Rate driver (1-5 stars)
GET    /rides/history           ? List past rides
```

**Driver APIs:**
```
POST   /drivers/{driverId}/location  ? Update location
GET    /drivers/{driverId}/requests  ? Get pending requests
PATCH  /drivers/{driverId}/request   ? Accept/reject request
PATCH  /drivers/{driverId}/status    ? Set online/offline
POST   /drivers/{driverId}/rate      ? Rate rider
```

**Example: Request a Ride**
```json
POST /rides/request
{
  "pickup_latitude": 37.7749,
  "pickup_longitude": -122.4194,
  "dropoff_latitude": 37.8044,
  "dropoff_longitude": -122.2712,
  "ride_type": "POOL"  // or UberX, Uber Black
}

Response:
{
  "ride_id": "ride_123456",
  "status": "MATCHING",
  "estimated_arrival_time": "4 minutes",
  "estimated_fare": 12.50
}
```

**Example: Update Driver Location (real-time)**
```json
POST /drivers/driver_123/location
{
  "latitude": 37.7749,
  "longitude": -122.4194,
  "accuracy": 10,  // meters
  "timestamp": 1718704500000
}

Response: {"status": "OK"}
// This is sent every 10-30 seconds while driver is online
```

---

## Step 3: Database Design

**Why multiple databases?**

| Use Case | Database | Why? |
|----------|----------|------|
| User profiles, ratings | PostgreSQL | Structured, queryable, transactional |
| Real-time location | Redis | Sub-millisecond access, geospatial queries (GEORADIUS) |
| Ride history, transactions | PostgreSQL + Archive | Durability, compliance, queryable |
| Ride events (matching, start, end) | Kafka + Time-series DB | Event sourcing, analytics |
| Driver search (nearby drivers) | Redis Geo index | Fast geospatial queries |

---

## Step 4: Data Schema

**Users Table:**
```sql
CREATE TABLE users (
  user_id VARCHAR PRIMARY KEY,
  name VARCHAR,
  phone VARCHAR UNIQUE,
  email VARCHAR UNIQUE,
  rating DECIMAL(3,1),  -- 1-5 stars
  total_rides INT,
  wallet_balance DECIMAL,
  payment_method VARCHAR,
  created_at TIMESTAMP,
  is_active BOOLEAN
);
```

**Drivers Table:**
```sql
CREATE TABLE drivers (
  driver_id VARCHAR PRIMARY KEY,
  name VARCHAR,
  phone VARCHAR UNIQUE,
  rating DECIMAL(3,1),
  vehicle_type VARCHAR,  -- UberX, Black, Pool
  license_number VARCHAR,
  insurance_expiry DATE,
  total_trips INT,
  total_earnings DECIMAL,
  status VARCHAR,  -- ONLINE, OFFLINE, BUSY
  created_at TIMESTAMP
);
```

**Rides Table:**
```sql
CREATE TABLE rides (
  ride_id VARCHAR PRIMARY KEY,
  rider_id VARCHAR FOREIGN KEY,
  driver_id VARCHAR FOREIGN KEY,
  pickup_location GEOMETRY,  -- (lat, long)
  dropoff_location GEOMETRY,
  pickup_time TIMESTAMP,
  dropoff_time TIMESTAMP,
  distance_km DECIMAL,
  duration_seconds INT,
  base_fare DECIMAL,
  surge_multiplier DECIMAL,  -- 1.0, 1.5, 2.0, etc.
  final_amount DECIMAL,
  rider_rating INT,  -- 1-5
  driver_rating INT,
  status VARCHAR,  -- REQUESTED, ACCEPTED, STARTED, COMPLETED
  created_at TIMESTAMP
);
```

**Real-time Location (Redis Geo):**
```
Redis command:
GEOADD drivers_location 37.7749 -122.4194 driver_123
GEOADD drivers_location 37.7750 -122.4195 driver_456

Query (find drivers within 5km):
GEORADIUS drivers_location 37.7749 -122.4194 5 km WITHCOORD WITHDIST
? [driver_123, 0.05km], [driver_456, 2.3km]
```

---

## Step 5: High-Level Architecture

![High-Level Architecture](diagrams/07-ride-sharing-backend-step5.svg)

```
+---------------------+
�  Mobile Apps        � (Rider/Driver)
+---------------------+
           �
    +------?----------+
    �  API Gateway    �
    �  Load Balancer  �
    +-----------------+
           �
    +--------------------------------+
    �                                �
+---?--------+  +----------------+  �
�Location     �  �Matching        �  �
�Service      �  �Service         �  �
+------------+  +----------------+  �
    �                �              �
    +----------------�              �
    �         �      �              �
+---?--+ +---?--+ +-?------+     +-?--------+
�Redis � �PG DB � �Kafka   �     �ETA Service
�Geo   � �      � �Events  �     �(Traffic) 
+------+ +------+ +--------+     +----------+

Services:
- Location Service: Reads from driver apps, updates Redis Geo
- Matching Service: Queries Redis Geo, selects best driver
- ETA Service: Calculates pickup/dropoff times using traffic data
- Kafka: Event stream (ride.requested, driver.accepted, ride.completed)
- Payment Service: Charges rider, pays driver (async)
```

---

## Step 6: Real-Time Location Tracking

**Driver App Flow:**
```
Every 10-30 seconds:
1. GET device location (GPS)
2. POST /drivers/{id}/location
3. Server updates Redis geo-index
4. Broadcast location to matching service

Effect:
- Rider can see driver on map in real-time
- Matching service has fresh driver positions
- ETA calculations are accurate
```

**Geospatial Indexing Options:**

| Option | How It Works | Pros | Cons |
|--------|-------------|------|------|
| **Quadtree** | Recursively divide map into 4 cells; store drivers in leaf nodes | Very fast nearby search; works well for uneven distribution | Complex to implement; hard to scale across regions |
| **Grid/GeoHashing** | Divide world into grid cells; map location to cell | Simple; scales across regions | Collision handling (drivers at cell boundary) |
| **Redis Geo** | Built-in geo-indexing using geohash | Simple; production-ready; good performance | Limited to one data type (single index) |

**Redis Geo Commands:**
```
GEOADD active_drivers 37.7749 -122.4194 driver_123
GEOADD active_drivers 37.7750 -122.4195 driver_456
GEOADD active_drivers 37.7760 -122.4100 driver_789

Find drivers within 5km:
GEORADIUS active_drivers 37.7749 -122.4194 5 km
? [driver_123, driver_456]

Find closest 3 drivers:
GEORADIUSBYMEMBER active_drivers driver_123 10 km COUNT 3
```

---

## Step 6.5: Real-World Scale�50,000 Concurrent Drivers (e.g., Chennai City)

**Practical Scenario:**
You're operating in Chennai, India. During rush hour (8pm), all 50,000 active drivers turn on the ride-sharing app. Each driver updates their location every 10-30 seconds. Can a single Redis Geo index handle this?

**Answer: Yes, easily�with proper engineering.**

---

### Write Throughput Calculation

**The Math:**
```
Total drivers: 50,000
Update frequency: 10-30 seconds (use 20s average)
Write throughput: 50,000 � 20 = 2,500 writes/second

Redis Geo capacity:
- Single Redis node: 200,000-500,000 operations/sec (depends on hardware, config, operation type)
- Conservative estimate for GEOADD: 50,000-100,000 writes/sec
- Your requirement: 2,500 writes/sec
- Headroom: 20-200x capacity available ?
- Conclusion: 1 Redis node more than sufficient (use 3 for HA + replication)
```

**What's actually stored in Redis:**
```
Key: "active_drivers_city:chennai"
Value: Sorted set of (longitude, latitude, driver_id)

Example entries:
driver_00001 ? (13.0827, 80.2707)   ? Updated 5 sec ago
driver_00002 ? (13.0828, 80.2708)   ? Updated 12 sec ago
driver_00003 ? (13.0829, 80.2710)   ? Updated 18 sec ago
... (50,000 entries)
Total memory: ~50MB (very small)
```

---

### Why NOT All 50,000 Update Simultaneously

**Common misconception:** "50,000 drivers turning on the app = 50,000 writes in 1 second = traffic spike!"

**Reality:**
```
Time (seconds)  Writes in that second   Why?
0               250                     Driver A updates
1               247                     Different drivers (randomized)
2               252                     Still spreading
3               251                     Natural jitter
4               248
...
10              2,500                   Steady state, averaged
20              2,500
30              2,500                   Back to driver A updating (20-30s cycle)
```

**Why is it spread?**
- Each driver has **independent location update intervals**
- Driver A updates at t=0, 20, 40, 60...
- Driver B updates at t=3, 23, 43, 63...
- Driver C updates at t=7, 27, 47, 67...
- etc.

**Result:** Updates are **naturally spread across the 10-30 second window**. Writes follow a Poisson distribution (random arrival times), not a synchronized thundering herd.

---

### Redis Capacity Sizing

**Single Redis node can handle:**
```
Write capacity:     200,000-500,000 ops/sec (modern hardware, optimized config)
                    Conservative: 50,000-100,000 writes/sec for GEOADD
Your throughput:    2,500 writes/sec
Headroom:           20-200x spare capacity ?

Read capacity:      200,000-500,000 ops/sec
Each ride request:  1-2 GEORADIUS queries
Peak ride requests: 50,000 QPS � 10 = 5,000 QPS (typically 10% of drivers request rides)
Your throughput:    5,000-10,000 reads/sec
Headroom:           20-100x spare capacity ?

Memory:             ~50MB for 50,000 drivers
                    + index overhead = ~100MB
                    Redis limit: 10-20GB available (uses <1%)
```

**Recommended setup for Chennai (production):**
```
Redis Cluster with 3 nodes (for HA):
- Node 1: Master (active_drivers_city:chennai)
- Node 2: Replica (read-only, failover candidate)
- Node 3: Replica (read-only, backup)

Load distribution:
- Writes: 100% to Master
- Reads (GEORADIUS): 80% from Replicas, 20% from Master
- Failover: If Master dies, automatic promotion to Replica

Cost: ~$300-500/month on AWS (r6g.large � 3)
```

---

### Handling Stale Data (Driver Goes Offline)

**Problem:**
```
Driver turns on app at 8pm, stays online until 8:30pm.
But at 8:31pm, driver goes offline (closes app).
Redis still shows driver as "available" for next 10-30 seconds.
Matching service assigns a ride to offline driver.
```

**Solution: TTL (Time-To-Live) on Location Data**

**Approach 1: GEOADD with EXPIRE**
**Interview Note:** Implementation details are intentionally omitted here; focus on design choices, tradeoffs, failure modes, and scaling rationale.

**Approach 2: Sync with PostgreSQL Status**
**Interview Note:** Implementation details are intentionally omitted here; focus on design choices, tradeoffs, failure modes, and scaling rationale.

**Timeline:**
```
t=0s:   Driver location update ? GEOADD + EXPIRE 40s
t=20s:  Driver still online, sends new update ? GEOADD + EXPIRE 40s (resets TTL)
t=30s:  Driver still online, sends new update ? GEOADD + EXPIRE 40s
t=35s:  Driver closes app (offline), no new update sent
t=40s:  Redis automatically expires driver from geo-index (TTL elapsed)
t=41s:  Rider requests ride, GEORADIUS query doesn't return this driver
        (Also PG check confirms status=OFFLINE)
```

---

### Handling Startup Spike (Crash Recovery)

**Problem:**
```
Redis crashes at 8pm.
After 2-minute recovery, 50,000 drivers re-sync their location to backend.
Expected: 2,500 writes/sec
Actual: 25,000 writes/sec (50,000 drivers � 2 seconds)
? Overwhelms Redis replica sync, causes cascading failures
```

**Solution: Client-Side Jitter & Exponential Backoff**

**Driver App Code:**
**Interview Note:** Implementation details are intentionally omitted here; focus on design choices, tradeoffs, failure modes, and scaling rationale.

**Server-Side Rate Limiting:**
**Interview Note:** Implementation details are intentionally omitted here; focus on design choices, tradeoffs, failure modes, and scaling rationale.

**Result after jitter + backoff:**
```
Without jitter:
t=0-2s:  Spike to 25,000 writes/sec (crash recovery)
         ? Redis replica falls behind
         ? Matching queries stale data
         ? Bad matches

With jitter:
t=0-40s: Requests spread randomly across recovery window
         ? Average 1,250 writes/sec (half normal)
         ? Redis replica catches up
         ? Matches accurate
```

---

### Real-Time Location Update Flow (Complete)

**Sequence with all components:**

```
Driver Location Update Flow (Production)
=========================================

1. Driver App (Client)
   Every 20 � 10 seconds (with jitter):
     - GPS.getLocation() ? (13.0827, 80.2707)
     - HTTP POST /drivers/{id}/location
     - If fails ? exponential backoff + retry

2. Location Service (Stateless, horizontal scaling)
   Receives POST request:
     a) Rate limit check (max 1 per 5 sec)
     b) GEOADD active_drivers_city:chennai <lat> <long> <driver_id>
     c) EXPIRE driver_loc_ts:{driver_id} 40  ? Auto-expiry on stale data
     d) UPDATE drivers SET 
          latitude=?, longitude=?, 
          last_location_update=NOW()
     e) r.publish('driver_location_updates', {...})  ? Kafka

3. Redis (Geo Index)
   Stores: 50,000 drivers with coordinates
   Supports: GEORADIUS queries (find nearby drivers)
   Evicts: Drivers not updated within 40 seconds

4. Matching Service (Async, event-driven)
   On Kafka message 'driver_location_updates':
     a) Get rider's location from request queue
     b) GEORADIUS active_drivers_city:chennai <rider_lat> <rider_long> 5 km
     c) For each returned driver:
          SELECT status FROM drivers 
          WHERE driver_id=? AND status='ONLINE'
     d) Score and rank drivers
     e) Send requests to top 3 drivers in parallel

5. Response to Rider
   Location Service returns:
     {
       "status": "MATCHING",
       "driver_id": "driver_123",
       "driver_lat": 13.0827,
       "driver_long": 80.2707,
       "eta_pickup_seconds": 240,
       "timestamp": 1718704500000
     }
```

---

### Monitoring & Alerting

**Key Metrics to Track:**

```
Metric Name                 | Target | Alert Threshold
--------------------------------|--------|------------------
Location writes/sec         | 2,500  | > 5,000
Location update latency p99 | < 50ms | > 200ms
Redis geo query latency p99 | < 20ms | > 100ms
Active drivers in index     | 50,000 | < 40,000 (10% stale)
Redis memory usage          | 100MB  | > 1GB
Stale location TTL events   | Normal | Spikes indicate network issues
Rate limit rejections       | 0-5%   | > 10% (possible DDoS)
Matching query success rate | > 95%  | < 90% (driver availability issue)
```

**Interview Focus (Monitoring Tradeoffs):**

**Interview Note:** Implementation details are intentionally omitted here; focus on design choices, tradeoffs, failure modes, and scaling rationale.

---

### Capacity Planning for Other Cities

**Scaling formula for any city:**

```
Parameters:
  D = number of concurrent drivers
  U = location update frequency (seconds)
  
Write throughput: D � U writes/second
Read throughput: (D � 10) � 2 reads/second  (10% ride requests, ~2 queries each)
Memory: D � 1MB

Example cities:

Mumbai (100K drivers):
  Writes: 100,000 � 20 = 5,000 writes/sec
  Memory: 100MB
  ? Redis cluster 2-3 nodes recommended

NYC (200K drivers):
  Writes: 200,000 � 20 = 10,000 writes/sec
  Memory: 200MB
  ? Redis cluster 3-5 nodes recommended

Global (50M drivers across regions):
  Writes: 50,000,000 � 20 = 2.5M writes/sec
  ? Redis sharded by region (Chennai: 1-3 nodes, Mumbai: 2-3 nodes, etc.)
  ? Geohashing with regional routing
```

---

### Global Scalability: Multi-Region Redis Strategy

**Question: Do we need multiple Redis servers based on geo-location?**

**Short Answer: Yes, absolutely�especially for global ride-sharing.**

---

#### When Single Redis Fails for Global Scale

**Problem with one centralized Redis:**
```
Single Redis in us-east-1 (N. Virginia)

Driver in Shanghai (China):
- Network latency: 150-200ms just to reach Redis
- POST /location takes 200ms + 150ms RTT = 350ms
- User sees slow response
- Battery drain on driver app (network idle time)

Driver in S�o Paulo (Brazil):
- Network latency: 200-250ms
- Total: 400+ ms per location update
- Unacceptable user experience

Failure scenario:
- Redis in us-east-1 goes down
- All drivers globally can't update location
- All riders can't request rides
- Global blackout (99.99% SLA violated)

Scale problem (OLD THINKING):
- 50M drivers globally � 2,500 updates/sec average
- Modern Redis: 200K-500K writes/sec (previously underestimated at 15K)
- Need: 50,000,000 � 2,500 = 20M updates/sec � 250K/sec per node = ~80 nodes for single centralized (still problematic due to latency)
```

---

#### Solution: Geo-Sharded Redis Architecture

**Strategy: One Redis Cluster per region**

```
+------------------------------------------------------------+
�                    Global Dispatch Layer                    �
�         (API Gateway with geo-routing)                     �
+-------------------------------------------------------------+
       �              �              �              �
       � Route by     � Route by     � Route by     � Route by
       � region       � region       � region       � region
       �              �              �              �
   +---?------+   +--?------+   +--?------+   +--?------+
   � US-EAST  �   �EU-WEST  �   �ASIA-SE  �   �LATAM    �
   �Redis x3  �   �Redis x3  �   �Redis x3  �   �Redis x3  �
   �50K drivers�   �40K driv. �   �60K driv. �   �30K driv. �
   �2.5K w/s   �   �2.0K w/s  �   �3.0K w/s  �   �1.5K w/s  �
   +----------+   +---------+   +---------+   +---------+
       �              �              �              �
   +---?------+   +--?------+   +--?------+   +--?------+
   � PG-US    �   � PG-EU   �   � PG-ASIA �   � PG-BR   �
   � Rides    �   � Rides   �   � Rides   �   � Rides   �
   +----------+   +---------+   +---------+   +---------+
```

**Benefits:**
- ? Latency: Each driver connects to nearest Redis (< 50ms)
- ? Throughput: 2.5K writes/sec per region << 15K capacity
- ? Failure isolation: US-EAST down doesn't affect EU-WEST drivers
- ? Scalability: Add more regions as needed

---

#### Regional Redis Cluster Setup

**Each region: 3-node Redis Cluster**

```
Region: us-east-1 (N. Virginia)

+-------------------------------------+
�   Redis Cluster (3 nodes)           �
�                                     �
�  Node 1 (Master)          Node 2    �
�  - Slot 0-5460            (Replica) �
�  - Writes only            - Read    �
�  - 2,500 w/s              access    �
�                                     �
�  Node 3 (Replica)                  �
�  - Slot 5461-10922        - Backup �
�  - Read-only              - Failover�
�  - Failover candidate              �
�                                     �
�  Replication Factor: 2              �
�  (each slot replicated on 2 nodes)  �
+-------------------------------------+

Same setup for each region:
- us-east-1 (Virginia, USA)
- eu-west-1 (Dublin, Ireland)
- ap-southeast-1 (Singapore)
- ap-northeast-1 (Tokyo, Japan)
- sa-east-1 (S�o Paulo, Brazil)
```

---

#### Regional Write/Read Flow

**Write: Driver Updates Location**

**Interview Note:** Implementation details are intentionally omitted here; focus on design choices, tradeoffs, failure modes, and scaling rationale.

---

#### Read: Matching Service Queries Nearby Drivers

**Interview Note:** Implementation details are intentionally omitted here; focus on design choices, tradeoffs, failure modes, and scaling rationale.

---

#### Capacity Per Region (Example)

```
Region            | Drivers | Update Freq | Writes/sec | Redis Nodes | Cost/month
------------------|------------|------------|------------|-------------|----------
us-east-1 (USA)   | 50,000    | 20s        | 2,500      | 3           | $450
eu-west-1 (EU)    | 40,000    | 20s        | 2,000      | 3           | $450
ap-southeast-1    | 60,000    | 20s        | 3,000      | 3           | $450
ap-northeast-1    | 30,000    | 20s        | 1,500      | 3           | $450
sa-east-1 (Brazil)| 20,000    | 20s        | 1,000      | 3           | $450
------------------|------------|------------|------------|-------------|----------
TOTAL GLOBAL      | 200,000   | 20s        | 10,000     | 15          | $2,250/month

Single-region capacity: 200,000-500,000 writes/sec (modern hardware)
Global throughput: 10,000 writes/sec
Headroom per region: 20-50x ?
Overall system: Scalable to 100x+ current size
```

---

#### Cross-Region Considerations (Rides Crossing Boundaries)

**Problem: Driver in US-East picks up rider in EU-West?**

```
Scenario:
- Driver location in US-East Redis
- Rider in US-East (matches fine)
- But during ride, driver crosses Atlantic to EU
- Driver arrives in EU, but location still in US Redis
- Breakdown in next ride matching

Solution: Regional Handoff

1. Driver crosses region boundary
2. Update occurs to new region Redis
3. Publish "driver_crossed_region" event
4. Matching service gets updated location
5. Next ride request matches in correct region
```

**Implementation:**

**Interview Note:** Implementation details are intentionally omitted here; focus on design choices, tradeoffs, failure modes, and scaling rationale.

---

#### Consistency & Eventual Consistency Trade-offs

**Strong Consistency (within region):**
```
? Driver location in Redis matches PostgreSQL (same region)
? Matching always has accurate driver data
? No race conditions within region
? Payments processed correctly (same region)

? Cross-region rides: brief inconsistency (10-30 seconds)
   Driver in old region Redis until next update
   But acceptable: cross-region rides are 1-2% of total
```

**Without Regional Sharding (Single Global Redis):**
```
? Strong consistency globally
? Single source of truth

? Latency: 150-200ms to distant Redis
? Capacity: need thousands of nodes
? Single point of failure: affects all regions
? Cost: 10x higher
```

---

#### Monitoring Multi-Region Redis

**Interview Focus: Regional Monitoring Tradeoffs**

**Interview Note:** Implementation details are intentionally omitted here; focus on design choices, tradeoffs, failure modes, and scaling rationale.

---

#### Cost Comparison: Single vs. Multi-Region

**Scenario: 200,000 drivers globally**

**Single Centralized Redis:**
```
Nodes needed: 200,000 drivers � 2,500 updates/sec
            � 250,000 writes/sec per node (modern capacity)
            � 1 node (with latency issue from 150-200ms for global users)

Cost:
- 6 Redis nodes � $150/month = $900/month
- BUT: latency 150-200ms for far drivers (bad UX)
- AND: single point of failure (any outage = global blackout)
```

**Multi-Region Redis (Recommended):**
```
Nodes needed per region: 3-5 (replication + failover)
Regions: 5 (US, EU, Asia-SE, Asia-NE, LatAm)
Total: 5 regions � 3 nodes = 15 nodes

Cost:
- 15 Redis nodes � $150/month = $2,250/month
- BUT: latency < 50ms for every driver (great UX)
- AND: regional failure isolated (US down ? EU down)
- AND: easy to scale (add regions/nodes independently)

ROI: Pay 2.5x more for:
  ? 3-4x latency improvement (150ms ? 50ms)
  ? 99.99% regional availability (vs. 99% global)
  ? Scalability to billions of drivers
  ? Compliance with data residency laws (EU data in EU, etc.)
```

---

#### Data Residency & Compliance

**GDPR, CCPA, etc. require data stored in specific regions:**

```
? Multi-region setup makes this easy:
   - EU drivers' data ? EU Redis + EU PostgreSQL
   - US drivers' data ? US Redis + US PostgreSQL
   - Brazil drivers' data ? Brazil Redis + Brazil PostgreSQL

? Single Redis would require complex routing:
   - All data in us-east-1 Redis
   - Violates GDPR (EU data must stay in EU)
   - Risk of fines: millions of dollars
```

---

#### Decision Matrix: When to Use Each Approach

| Criteria | Single Redis | Multi-Region Redis |
|----------|------|----------|
| Global drivers | < 10,000 | > 10,000 |
| Regional latency requirement | < 100ms acceptable | < 50ms required |
| Operational complexity | Low | Medium-High |
| Cost | $900/month | $2,250/month |
| Failure isolation | No (global blackout) | Yes (regional only) |
| Data residency compliance | Difficult | Easy |
| Scalability limit | ~200K drivers | Billions (add regions) |
| Interview readiness | Good | Excellent |

**For ride-sharing: Always use Multi-Region (>99% of cases)**

---

## Step 7: Matching Algorithm

**Problem:**
- Rider requests at (37.7749, -122.4194)
- 500 available drivers nearby
- Which driver should we assign?

**Solution: Ranked Scoring**
```
Score = 0.4 � Distance_Factor + 
         0.3 � Rating_Factor +
         0.2 � Acceptance_Rate +
         0.1 � Recent_Activity

Example for driver_123:
- Distance to rider: 2 km (0.6 score out of 1)
- Rating: 4.8 stars (0.96 score out of 1)
- Acceptance rate: 95% (0.95 score)
- Activity: active last 1 min (1.0 score)

Final Score = 0.4�0.6 + 0.3�0.96 + 0.2�0.95 + 0.1�1.0
           = 0.24 + 0.288 + 0.19 + 0.1
           = 0.818
```

**Matching Process:**
```
1. Query Redis Geo: Find drivers within 5km
   ? [driver_123, driver_456, driver_789, ...]

2. Rank drivers by score
   ? driver_123: 0.818
   ? driver_456: 0.792
   ? driver_789: 0.715

3. Send request to top 3 drivers in parallel
   ? "You have a new ride request from A to B"

4. Wait for acceptance (30-second timeout)
   ? Driver 123 accepts: ? Match created
   ? Others: cancel their requests

5. If no one accepts, expand radius (5km ? 7km ? 10km)
   or lower rating threshold (4.0 ? 3.8)
```

---

## Step 8: Surge Pricing

**Why surge pricing?**
```
Normal evening: 1000 riders, 500 drivers available
? Average wait: 30 seconds
? No surge needed

Peak evening: 10,000 riders, 500 drivers available
? Demand >> Supply
? Average wait: 10 minutes
? Riders cancel, drivers get overwhelmed
? Solution: Raise prices to reduce demand, attract more drivers
```

**Surge Calculation:**
```
Surge Factor = Demand / Supply

Demand = Requests queued + Current active
Supply = Available drivers

Example:
- Demand: 500 (100 active + 400 waiting)
- Supply: 100 drivers
- Surge Factor: 500 / 100 = 5x

Price = Base Price � Surge Factor
Base: $10
Surge: 5x
Final: $50 per ride
```

**Surge Tiers:**
```
Ratio 1.0-1.5:  1.0x (normal)
Ratio 1.5-3.0:  1.5x
Ratio 3.0-5.0:  2.0x
Ratio 5.0+:     3.0x

Update frequency: Every 1-5 minutes
Benefits:
- Attracts drivers to surge areas
- Reduces rider demand
- Balances supply/demand
Downsides: Social inequality (poor users can't afford)
```

---

## Step 9: ETA Calculation

**Components:**
```
1. Get driver location & route to rider
2. Calculate pickup ETA using:
   - Distance
   - Current traffic (real-time)
   - Historical patterns (typical travel time)
   - Time of day (rush hour slower)

3. Get dropoff location & calculate drop ETA
   
4. Total = Pickup ETA + Trip Duration ETA

Example:
- Driver 2km away, normal traffic: 5 min pickup
- Trip 10km, rush hour traffic: 25 min duration
- Total ETA: 30 min
```

**Data Sources:**
```
Historical:
- Past trips on this route
- Time-of-day patterns (8am rush vs 3am quiet)
- Day-of-week patterns (Friday different from Tuesday)
- Seasonal patterns (holiday traffic)

Real-time:
- GPS data from active drivers (congestion detection)
- Google Maps/Weather API (traffic incidents)
- Weather conditions (rain slows drivers)
```

---

## Step 10: Real-Time Communication

**WebSocket for Live Updates:**
```
Client ? Server: 
{
  "type": "subscribe_ride",
  "ride_id": "ride_123"
}

Server ? Client (every 10 seconds):
{
  "type": "driver_location_update",
  "driver_lat": 37.7759,
  "driver_long": -122.4184,
  "eta_seconds": 240,
  "driver_name": "John",
  "driver_rating": 4.8,
  "vehicle": "Honda Civic (ABC-1234)"
}

Fallback: Long polling if WebSocket fails
- Client polls server every 5 seconds
- Less efficient but works everywhere
```

---

## Step 11: Payment Processing

**Payment Flow:**
```
1. Ride completes at dropoff
   - Calculate fare = base + distance + time + surge
   - Example: $10 + $2 + $3 + $0 (no surge) = $15

2. Charge rider:
   - Pre-authorized credit card
   - Deduct from wallet
   - Log transaction

3. Settlement (hourly or daily):
   - Driver gets 75% of fare: $11.25
   - Platform commission: 25%: $3.75
   - Driver tips paid separately (100% to driver)

4. Reconciliation:
   - Audit all transactions
   - Handle refunds/disputes
   - Tax reporting
```

---

## Step 12: Rating & Reviews

**Two-sided ratings:**
```
After ride:
1. Rider rates driver (1-5 stars, optional comment)
2. Driver rates rider (1-5 stars, optional comment)

Storage:
{
  "ride_id": "ride_123",
  "rider_id": "rider_456",
  "driver_id": "driver_789",
  "rider_rating_of_driver": 5,
  "rider_comment": "Great driver!",
  "driver_rating_of_rider": 4,
  "driver_comment": "Good rider",
  "timestamp": "2024-06-18"
}

Aggregation:
- Driver rating = AVG(all ratings received)
- Displayed on driver profile
- Affects matching (high-rated drivers prioritized)
```

**Low rating handling:**
```
Driver < 4.5 stars: Warning visible in app, less visibility
Driver < 4.0 stars: Potentially deactivated, must appeal
Fraud: Monitor for suspicious patterns
```

---

## Architecture Decision Q&A with Tradeoffs

This section collects critical architectural decisions for ride-sharing systems. Each question has multiple valid approaches�the "best" choice depends on your constraints.

---

### Decision 1: Multi-Region vs Single-Region Redis

**Question: Do we need multiple Redis servers based on geo-location?**

| Aspect | Single-Region | Multi-Region |
|--------|------|----------|
| **Latency** | 150-200ms for distant drivers ? | < 50ms everywhere ? |
| **Cost** | $900/month (6 nodes) | $2,250/month (15 nodes) |
| **Failure mode** | Global blackout (all regions down) ? | Regional only (1/5 regions down) ? |
| **Operational complexity** | Simple (1 cluster) | Medium (5 clusters + routing) |
| **GDPR compliance** | Difficult (EU data in US) ? | Easy (EU data in EU) ? |
| **Scalability limit** | ~200K drivers | Billions (add regions) |
| **When to choose** | < 10K drivers, budget-constrained | > 50K drivers globally, performance critical |

**Decision Rule:**
```
IF (global_drivers > 50,000) OR (latency_requirement < 100ms) OR (GDPR_required)
  THEN use Multi-Region
ELSE use Single-Region (for now, migrate later)
```

---

### Decision 2: Strong Consistency vs Eventual Consistency for Metadata

**Question: Should driver location always match exactly between Redis and PostgreSQL?**

| Aspect | Strong Consistency | Eventual Consistency |
|--------|------|----------|
| **Guarantee** | Redis = PostgreSQL always | Brief window (10-30s) of mismatch possible |
| **Implementation** | Sync writes to both; transaction | Async events to multiple systems |
| **Latency** | 100-150ms (multiple writes) | 50ms (single write) |
| **Matching accuracy** | 100% correct driver list ? | 95-99% correct (stale data) |
| **Failure handling** | If Redis write fails, whole request fails | If PostgreSQL slow, Redis updates anyway |
| **Cost** | Higher (strong coordination overhead) | Lower (simple async) |
| **When to choose** | Payment, ACL, critical state | Location, cache, analytics |

**Decision Rule:**
```
Strong Consistency for:
  - Payment/billing data
  - User authentication
  - Access control (ACL)
  - Ride state transitions (ACCEPTED ? STARTED)

Eventual Consistency for:
  - Driver location (acceptable 30s staleness)
  - Search index
  - Cache
  - Analytics data
```

---

### Decision 3: SQL (PostgreSQL) vs NoSQL (MongoDB) for Metadata

**Question: Should we use PostgreSQL or MongoDB for driver/rider metadata?**

| Aspect | PostgreSQL (SQL) | MongoDB (NoSQL) |
|--------|------|----------|
| **Transactions** | ACID, multi-row ? | Single-doc transactions (limited) |
| **Queries** | Complex joins, aggregations | Key-value lookups only |
| **Schema** | Strict (migrations needed) | Flexible (schema-less) |
| **Scaling** | Vertical, then sharding (complex) | Horizontal auto-sharding ? |
| **Consistency** | Strong (immediate) | Eventually consistent |
| **Cost** | ~$300-500/month (3 nodes, HA) | ~$500/month (sharded cluster) |
| **When to choose** | Relational data, transactions needed | High write scale, no joins |

**Decision Rule:**
```
Use PostgreSQL for:
  - drivers (id, rating, status, license, insurance)
  - users (id, rating, payment_method)
  - rides (rider_id, driver_id, pickup, dropoff, fare)
  - Requires: foreign keys, ACL updates, billing accuracy

Use MongoDB for:
  - driver_location_history (archive, no queries on other fields)
  - user_preferences (read-only, no joins)
  - ride_events (append-only, massive scale)
```

---

### Decision 4: Location Update Frequency: 10s vs 20s vs 30s

**Question: How often should drivers update their location?**

| Update Freq | Battery Impact | ETA Accuracy | Server Load | Latency Impact |
|--------|------|----------|------|----------|
| **Every 5s** | ?????? Drains fast | ? Very accurate | ?? 20K w/sec (50K drivers) | 50-100ms |
| **Every 10s** | ???? Moderate drain | ? Accurate | ?? 5K w/sec | 30-50ms |
| **Every 20s** | ?? Acceptable | ?? Acceptable | ? 2.5K w/sec | 20-30ms |
| **Every 30s** | ? Best | ?? Inaccurate | ? 1.7K w/sec | 15-20ms |

**Decision Rule:**
```
10-20 seconds is optimal for ride-sharing:
  - Balance: battery (not too frequent) + accuracy (not too stale)
  - 20s chosen because:
    * Median pickup time: 5-7 min = 15-20 location updates
    * ETA error budget: 1-2 location intervals acceptable
    * Server load: 2,500 w/sec << 200,000+ capacity (huge headroom)
    * Driver experience: responsive but not battery drain
```

**Implementation:**
**Interview Note:** Implementation details are intentionally omitted here; focus on design choices, tradeoffs, failure modes, and scaling rationale.

---

### Decision 5: Send Requests to 3 vs 5 vs 10 Drivers

**Question: How many drivers should we request in parallel for each ride?**

| # of Drivers | Acceptance Rate | Latency | Server Load | UX |
|--------|------|----------|------|----------|
| **1** | 70% ? 30% success | 30s (if reject) ? | Low | Poor (many fails) |
| **3** | 70% � 70% � 70% = 34% ? 85% success | 5-10s | Medium | Good |
| **5** | 70%^5 = 17% ? 94% success | 5-10s | Medium-High | Very good |
| **10** | 70%^10 = 2.8% ? 99% success | 5-10s | High | Annoying (too many requests) |

**Decision Rule:**
```
Send to 3 drivers because:
  - First one accepts: match found immediately ?
  - One rejects: second has 70% chance = 91% cumulative ?
  - Two reject: third has 70% chance = 97% cumulative ?
  - More than 3: driver overload (too many notifications) ?
  
Trade-off: Accept 97% success rate instead of 99% to avoid driver overload
```

---

### Decision 6: Surge Pricing: Update Every 1min vs 5min vs 15min

**Question: How often should surge pricing recalculate?**

| Frequency | Accuracy | Driver Response Time | System Load | Fairness |
|--------|------|----------|------|----------|
| **Every 30s** | ?? Very accurate | ? Immediate | ?? High | ?? Constant fluctuation |
| **Every 1min** | ? Accurate | ? Quick | ? Medium | ? Good |
| **Every 5min** | ?? Acceptable | ?? Slow | ? Low | ?? Rider waits for price drop |
| **Every 15min** | ?? Stale | ? Very slow | ? Very low | ? Unfair |

**Decision Rule:**
```
Update every 1-5 minutes because:
  - Faster (< 1min): Drivers gaming the system (go online before surge)
  - Slower (> 10min): Pricing stale, riders wait for drop, matches happen at old price

Recommended: 2-3 minutes
  - Captures demand spikes quickly
  - Enough time for driver decisions to propagate
  - Reduces gaming
```

---

### Decision 7: Cache Invalidation: TTL vs Event-Based vs Time-Based

**Question: When should we clear cached driver data?**

| Strategy | Consistency | Latency | Complexity | Memory Usage |
|--------|------|----------|------|----------|
| **TTL Only** | Eventual (wait for expiry) | Fast (Redis hit) | Simple ? | Predictable |
| **Event-Based** | Strong (immediate invalidate) | Slower (listen + purge) | Complex | Lower (immediate cleanup) |
| **Hybrid** | Very strong | Fast | Medium | Predictable + responsive |

**Decision Rule:**
```
Use Hybrid Approach (Recommended):
  1. TTL: All cache keys expire after 40-60 seconds (safety net)
  2. Event-Based: Publish "driver_went_offline" ? purge immediately
  3. Benefits: 
     - Fast for happy path (TTL)
     - Strong consistency for offline (event-based)
     - No memory leaks (TTL cleanup)
```

---

### Decision 8: Database Sharding: By City vs By Driver ID vs By Ride ID

**Question: How should we partition the database for scale?**

| Strategy | Hotspots | Rebalancing | Cross-Shard Queries |
|--------|------|----------|------|
| **By City** | Rush hour in one city ? | Easy (city grows) ? | None (queries per-city) ? |
| **By Driver ID** | Random distribution | Hard (must re-shard) | Many (riders across shards) ? |
| **By Ride ID** | Random distribution | Hard | Many (multi-shard joins) ? |

**Decision Rule:**
```
Shard by City (Region) because:
  - Natural boundaries (SF, NYC, London, etc.)
  - Queries are per-city (riders in SF match drivers in SF)
  - Hotspots handled by regional load (surge pricing works city-level)
  - Rebalancing: add new shard when city grows
  
Trade-off: Cross-city rides (rare) need special handling
```

---

### Decision 9: Idempotency: UUID vs Request-ID vs Timestamp-Based

**Question: How do we prevent duplicate rides if requests retry?**

| Strategy | Deduplication | Storage | Lookup Speed | Failure Recovery |
|--------|------|----------|------|----------|
| **UUID** | rider_id + request_uuid | Dedupe table | ?? Fast | ? Good |
| **Request-ID** | request_id (external) | Same | ?? Fast | ? Good |
| **Timestamp** | rider_id + timestamp | Dedupe table | ?? Slower | ?? Collisions possible |

**Decision Rule:**
```
Use Request-ID because:
  - Client generates globally unique ID (UUID)
  - Client sends same ID on all retries
  - Server deduplicates on (user_id, request_id, operation)
  - Return cached response if duplicate
  
Interview framing:
  POST /rides/request {request_id: "uuid-123", ...}
  Server: Check if (user_id, request_id) exists ? return cached result
```

---

### Decision 10: Real-Time Communication: WebSocket vs Long-Polling vs Polling

**Question: How should riders see real-time driver location updates?**

| Strategy | Latency | Bandwidth | Complexity | Fallback |
|--------|------|----------|------|----------|
| **WebSocket** | 100-200ms ? | 50% less | Medium (stateful) | ?? Fallback needed |
| **Long-Polling** | 500-1000ms | 2x more | Simple | ? Works everywhere |
| **Short-Polling** | 5-10 seconds | 10x more | Simple | ? Works everywhere |

**Decision Rule:**
```
Use WebSocket with Long-Polling Fallback (Recommended):
  - WebSocket: Primary (fast, efficient)
  - Long-polling: Fallback if WebSocket fails
  - Short-polling: Last resort only
  
Trade-off: WebSocket stateful (more server resources)
           but performance gain worth it (100ms vs 1s latency)
```

---

### Decision 11: Payment Processing: Synchronous vs Asynchronous

**Question: Should we charge riders immediately or asynchronously?**

| Approach | User Feedback | Correctness | Recovery | Server Load |
|--------|------|----------|------|----------|
| **Synchronous** | Immediate (fast) ? | Strong (no delays) ? | Simple (retry request) | Lower |
| **Asynchronous** | Delayed (2-5s) ? | Weak (failures hidden) | Complex (events + reconciliation) | Higher |

**Decision Rule:**
```
Use Synchronous for ride-sharing because:
  - User needs instant feedback ("Payment success")
  - Driver must be paid before next ride (trust)
  - Failure immediately visible ? user can retry
  
Exception: Very high-volume (1M+ concurrent):
  - Use async job queue for excess load
  - Keep 80% sync + 20% async for peak shaving
```

---

### Decision 12: Matching Algorithm: Deterministic vs Probabilistic vs ML

**Question: Should matching be rule-based or ML-driven?**

| Approach | Accuracy | Latency | Interpretability | Bias Risk |
|--------|------|----------|------|----------|
| **Rules** (distance + rating) | 85% | 50ms ? | 100% ? | Manual tuning |
| **Probabilistic** (weighted random) | 88% | 100ms | 70% | Reduced by randomness ? |
| **ML Model** | 95% | 200ms+ | 0% ? | High risk ? |

**Decision Rule:**
```
Start with Rules-Based (recommended for MVP):
  - Score = 0.4�distance + 0.3�rating + 0.2�acceptance_rate + 0.1�activity
  - Simple, explainable, debuggable
  - Good enough (85-90% success rate)

Add Probabilistic (when scale increases):
  - Randomly select from top 3 scored drivers
  - Prevents gaming (drivers gaming "best score")
  - Still interpretable

Add ML only if:
  - Data-driven improvements > 5% (proven in experiments)
  - Can explain model decisions (bias audit)
  - Enough data collected (6+ months history)
```

---

### Decision 13: Ride History Storage: Hot vs Warm vs Cold

**Question: Where should we store completed ride records?**

| Tier | Query Speed | Cost/GB | Use Case | Retention |
|--------|------|----------|------|----------|
| **Hot** (PostgreSQL) | 100ms ? | $50/GB | Last 30 days | 1 month |
| **Warm** (S3 + Athena) | 5-30s | $1/GB | 1-12 months | 1 year |
| **Cold** (Glacier) | 1-5 hours | $0.004/GB | Compliance | 7 years |

**Decision Rule:**
```
Tiered storage:
  1. Hot (PostgreSQL): Last 30 days (active users check history)
  2. Warm (S3+Athena): 30 days - 1 year (analytics, disputes)
  3. Cold (Glacier): 1-7 years (compliance, tax audits)

Benefits: 
  - 95% queries hit hot tier (fast)
  - Rare old queries use warm (acceptable delay)
  - Legal requirement (cold storage)
  - Cost-optimized: $X hot + $Y warm + $Z cold << all hot
```

---

### Decision 14: Circuit Breaker Thresholds: 50% vs 70% Error Rate

**Question: When should we open the circuit breaker for a failing service?**

| Threshold | False Positives | Recovery Time | User Impact |
|--------|------|----------|------|----------|
| **30%** | High (flaky services trip easily) ? | Fast | Frequent fallback |
| **50%** | Medium | 10-20s | Balanced ? |
| **70%** | Low (might not trip even if service broken) | Slow (delays failure detection) | High (users see errors) ? |

**Decision Rule:**
```
Use 50% error rate threshold because:
  - At 50%, service is clearly broken (not flaky)
  - Recovery before 70% keeps user experience acceptable
  - Typical: trip on 5 errors in 10-request window
  
Parameters:
  - Open on: 50% errors (5 failures / 10 requests)
  - Half-open on: 30s timeout
  - Close on: 100% success in half-open (all 3 test requests succeed)
```

---

### Decision 15: Retry Strategy: Linear vs Exponential vs Fixed Backoff

**Question: How should we retry failed requests?**

| Strategy | Max Delay | Thundering Herd | Fairness |
|--------|------|----------|------|
| **Fixed** (retry every 100ms) | Unbounded | High (all retry together) ? | Bad |
| **Linear** (100ms, 200ms, 300ms) | 500ms + 3 retries | Medium | Fair |
| **Exponential** (100ms, 200ms, 400ms, 800ms) | 1600ms + jitter | Low ? | Best ? |

**Decision Rule:**
```
Use Exponential with Jitter (Recommended):
  Retry 1: 100ms + random(0-50ms)
  Retry 2: 200ms + random(0-100ms)
  Retry 3: 400ms + random(0-200ms)
  Retry 4: 800ms + random(0-400ms)
  Retry 5: 1600ms + random(0-800ms)
  
Why jitter: Prevents synchronized retries (thundering herd)
Max wait: ~3 seconds before giving up
```

---

## Interview Cheat Sheet Q&A

**Q: Why not compute matching on rider's phone?**  
A: Rider's phone doesn't know where drivers are�only server has that data. Also, driver availability changes constantly. Server needs global view to make good matching decisions. Client-side matching would require downloading all driver locations (millions), impractical.

**Q: What if driver accepts but then goes offline?**  
A: System detects no location updates > 5 minutes ? mark driver OFFLINE, cancel ride, reassign to another driver. Show rider "Finding another driver...". Not ideal but better than stuck driver.

**Q: Can we predict demand and pre-position drivers?**  
A: Yes! ML models predict: "Downtown surge likely at 6pm Friday." Notify drivers: "Go to downtown, expect surge surge in 1hr." Incentivize with bonus. Reduces wait time. But predictions are imperfect; if wrong, drivers waste time.

**Q: Why multipart location updates instead of continuous streaming?**  
A: Streaming consumes battery. Updates every 10-30 sec is good balance: fresh enough for accurate matching, not too frequent to drain battery. Also, server can't handle location stream from millions of drivers (bandwidth).

**Q: What if driver far away accepts request faster than nearby driver?**  
A: Send request to top 3 drivers in parallel. Whichever accepts first wins. Closer drivers have higher priority, but if they're slow to accept, farther driver can take it. First-come-first-served within the 30-second window.

**Q: How to detect and prevent fraud (false location, fake trips)?**  
A: Detect deviations from normal patterns. If driver "teleports" 500km in 1 sec, flag as suspicious. Monitor acceptance patterns (driver always accepts then cancels). Unusual payment patterns. Flag for manual review.

---

## Full Flow (Start to End)

### Happy Path
1. Client request enters API Gateway and is authenticated/authorized.
2. Orchestrator service validates input and routing context.
3. Core service executes primary business logic and required checks.
4. Read path uses cache first; fallback goes to durable database/store.
5. Write path updates fast layer first (where applicable) and publishes async events.
6. Downstream consumers persist durable state and trigger secondary effects.
7. Response is returned to client with final status and metadata.

### Failure and Retry Paths
1. Cache miss: read from durable store, then repopulate cache.
2. Dependency timeout: retry with backoff or circuit-breaker fallback.
3. Async event failure: retry queue and dead-letter queue (DLQ) handling.
4. Duplicate request: idempotency key returns prior successful outcome.
5. Concurrent updates: version/lock conflict triggers re-read and safe retry.

### End-State Guarantees
- Low-latency user operations on the hot path.
- Durable correctness in the source-of-truth datastore.
- Eventual consistency for non-critical async side effects.
- Strict correctness at critical boundaries (commit/payment/finalization).

---
## Summary

A ride-sharing system requires:
- ? Real-time geospatial indexing (Redis Geo or similar)
- ? Efficient matching algorithm with scoring
- ? Dynamic surge pricing for supply/demand balance
- ? Accurate ETA calculation using traffic data
- ? Real-time communication (WebSocket)
- ? Reliable payment processing
- ? Two-sided rating system
- ? Fraud detection and monitoring
- ? Global scalability (handle millions of concurrent users)


---

## What Are We Building?

A ride-sharing platform backend that handles:
- Real-time driver and rider matching
- Location tracking
- Route optimization
- Payment processing
- Rating and reviews
- High-volume concurrent requests

**Key Challenges:**
- Real-time location updates from millions of drivers
- Efficient matching algorithm at scale
- Handling traffic and high demand
- ETA calculation
- Fault tolerance and reliability

---

## Step 1: Requirements

### Functional Requirements
- Users can request a ride
- Drivers can accept ride requests
- Real-time location tracking
- ETA calculation
- Pickup and drop-off management
- Payment processing
- Driver and rider ratings
- Ride history
- Notifications

### Non-Functional Requirements
- 1M+ concurrent users
- Support peak traffic (5-10x normal)
- Sub-second location updates
- Latency < 200ms for matching
- 99.99% availability
- Global coverage
- Real-time processing

---

## Step 2: Architecture Overview

```
+----------------------+
�  Mobile Apps         � (Driver, Rider)
+----------------------+
       �
   +---?----------------------+
   �  API Gateway             �
   �  (Load Balance, Auth)    �
   +--------------------------+
       �          �
   +---?---------------------+
   �                         �
+--?------+      +----------?---+
�Location �      �Matching       �
�Service  �      �Service        �
+----------+     +---------------+
   �                  �
   +-----------------�
   �      �          �
+--?--+ +-?---+ +---?----+
�Cache� � DB  � �Payment  �
�Geom � �     � �Service  �
+-----+ +-----+ +---------+

Message Queue: Kafka (events)
```

---

## Step 3: Core Services

### 1. Location Service
- Real-time tracking of driver locations
- Frequent updates (every 10-30 seconds)
- Store in cache for fast access
- Geospatial indexing

### 2. Matching Service
- Match drivers to riders based on location
- Priority: closest driver, acceptance rate, rating
- Handle surge pricing
- Allocate drivers efficiently

### 3. ETA Service
- Calculate time to pickup/drop-off
- Use historical traffic data
- Account for current traffic conditions
- Update in real-time

### 4. Payment Service
- Process payments
- Split payments (if shared ride)
- Handle surges
- Refunds

### 5. Notification Service
- Push notifications to drivers/riders
- In-app notifications
- Real-time updates

---

## Step 4: Location Management

### Location Tracking

```
Driver Updates Location Every 10-30 Seconds:
POST /location
Body: {
  "userId": "driver_123",
  "latitude": 37.7749,
  "longitude": -122.4194,
  "timestamp": 1624050000
}

Storage:
- Redis for hot data (last location of all drivers)
- NoSQL DB for historical data
- Real-time stream to matching service
```

### Geospatial Indexing

```
Redis Geo Commands:
GEOADD drivers_locations 13.361389 38.115556 "Palermo"
GEOPOS drivers_locations "Palermo"
GEODIST drivers_locations "Palermo" "Catania"
GEORADIUS drivers_locations 15 37 200 km

Find all drivers within radius:
GEORADIUS drivers_locations 37.7749 -122.4194 10 km
? Returns drivers within 10km
```

### Location Storage

```
Option 1: Quad Tree
- Divide map into quadrants
- Recursively divide cells
- Store drivers in leaf nodes
- Fast nearby search

Option 2: Grid (GeoHashing)
- Divide world into grid cells
- Map location to cell
- Fast retrieval
- Collision handling (nearby cells)

Option 3: Geohash
- Encode lat/long into single string
- Nearby locations have similar prefix
- "ezs42" represents area
- Great for indexing
```

---

## Step 5: Matching Algorithm

### Problem: Match Rider to Driver

```
Rider requests ride at (37.7749, -122.4194)
Hundreds of available drivers nearby
Which driver to assign?
```

### Matching Strategy

```
1. Candidate Selection:
   - Query drivers within 5-10 km radius
   - Filter by rating (> 4.0 stars)
   - Filter by vehicle type match
   - Filter by driver acceptance rate

2. Ranking:
   Priority = 0.4 � Distance + 
              0.3 � Rating +
              0.2 � Acceptance Rate +
              0.1 � Current Load

3. Send Request:
   - Send to top 3-5 candidates
   - First to accept gets match
   - Timeout: 30 seconds

4. Matching:
   - Create ride record
   - Notify driver and rider
   - Update ETA
```

### Handling No Match
```
If no driver accepts:
1. Expand search radius (5km ? 7km ? 10km)
2. Relax rating threshold (4.0 ? 3.8 ? 3.5)
3. Offer surge pricing to attract drivers
4. Queue rider, wait for available driver
```

---

## Step 6: Surge Pricing

### Dynamic Pricing

```
Base Rate: $5 + $0.5/km + $0.3/min
Surge Factor: Demand / Supply ratio

During high demand:
- More rider requests than available drivers
- Surge multiplier: 1.5x, 2x, 3x+
- Higher price attracts more drivers

Calculation:
Price = Base Price � Surge Multiplier

Example:
Base: $10
Surge: 2x
Final: $20
```

### Surge Detection

```
Metrics:
- Requests waiting: 100+
- Available drivers: < 10
- Ratio: 100/10 = 10 (10x demand)

Surge Tiers:
- Ratio 1-1.5: 1x (normal)
- Ratio 1.5-3: 1.5x
- Ratio 3-5: 2x
- Ratio 5+: 3x

Update frequency: Every 1-5 minutes
```

---

## Step 7: ETA Calculation

### Route Optimization

```
1. Get current location:
   - Driver location
   - Rider location
   - Traffic conditions

2. Calculate routes:
   - Pickup route (driver ? rider)
   - Drop-off route (rider ? destination)

3. Estimate times:
   - Historical data: avg time for route
   - Current traffic: real-time delay factor
   - Time of day: rush hour vs off-peak

4. Return ETAs:
   - Time to pickup: 5 min
   - Trip duration: 15 min
   - Total: 20 min
```

### Data Sources

```
Historical Data:
- Previous trips on this route
- Time of day patterns
- Day of week patterns
- Seasonal patterns

Real-time Traffic:
- GPS data from drivers
- Third-party API (Google Maps, etc.)
- Traffic incidents (accidents, construction)
- Weather conditions
```

---

## Step 8: Data Model

### Interview Answer: How Many Tables?

```
MVP (minimum): 4 SQL tables + Redis Geo
- users
- drivers
- rides
- payments

Production-ready: 8 SQL tables + Redis + Kafka events
- users
- drivers
- vehicles
- rides
- ride_events
- payments
- ratings
- driver_status_history
```

### Recommended Table Structure (Production)

1) users
```
user_id (PK), name, phone, email, rating, status, created_at
```

2) drivers
```
driver_id (PK), name, phone, rating, status, created_at
```

3) vehicles
```
vehicle_id (PK), driver_id (FK UNIQUE), type, plate_no, capacity, color
```

4) rides
```
ride_id (PK), rider_id (FK -> users.user_id), driver_id (FK -> drivers.driver_id),
pickup_location, dropoff_location, status, base_fare, surge_multiplier,
final_amount, requested_at, accepted_at, started_at, completed_at
```

5) ride_events
```
event_id (PK), ride_id (FK -> rides.ride_id), event_type, event_time, payload_json
```

6) payments
```
payment_id (PK), ride_id (FK UNIQUE -> rides.ride_id), payer_user_id (FK -> users.user_id),
amount, method, status, txn_ref, created_at
```

7) ratings
```
rating_id (PK), ride_id (FK -> rides.ride_id), reviewer_type, reviewer_id,
target_type, target_id, stars, comment, created_at
```

8) driver_status_history
```
status_id (PK), driver_id (FK -> drivers.driver_id), status, city_id, changed_at
```

### Mapping Across Tables (Cardinality)

```
users 1:N rides
drivers 1:N rides
drivers 1:1 vehicles
rides 1:N ride_events
rides 1:1 payments
rides 1:N ratings (typically max 2: rider->driver and driver->rider)
drivers 1:N driver_status_history
```

### Non-SQL Stores (Important for Interview)

```
Redis Geo:
- active driver location index for nearby search
- not a relational table; optimized for low-latency geospatial lookup

Kafka ride_events topic:
- async event stream for analytics, notifications, and replay

Optional location history store (NoSQL/object storage):
- stores route polyline points for completed rides
```

---

## Step 9: Real-time Communication

### WebSocket Connection

```
Client ? Server: Establish WebSocket
Server ? Client: Real-time updates

Updates sent:
- Driver location changes
- ETA updates
- Ride status changes
- Notifications
- Chat messages

Benefits:
- Low latency (< 100ms)
- Bidirectional communication
- Persistent connection
- Reduced polling overhead
```

### Fallback for Unstable Connections
```
Primary: WebSocket
Fallback: Long polling
- Client polls server every 5 seconds
- Slower but works on restricted networks

Or pub/sub messaging:
- Driver publishes location updates
- Rider subscribes to updates
- Scalable to millions
```

---

## Step 10: Payment Processing

### Payment Flow

```
1. Ride Completed:
   Calculate final amount:
   - Base fare
   - Distance
   - Time
   - Surge multiplier
   - Tolls/Taxes

2. Payment Processing:
   - Charge rider (pre-authorized card)
   - Mark as processed
   - Store transaction

3. Settlement:
   - Driver: 75% of fare + tips
   - Platform: 25% commission
   - Process daily/weekly

4. Reconciliation:
   - Match transactions with rides
   - Handle refunds/disputes
   - Audit for fraud
```

### Payment Methods
```
Credit/Debit Card:
- Primary payment method
- PCI-DSS compliant
- Tokenization for security

Cash:
- Rider pays driver in cash
- Driver marks as paid
- Tracked in system

Wallet:
- Pre-load balance
- Faster checkout
- Less friction
```

---

## Step 11: Rating & Reviews

### Rating System

```
After ride completion:
- Rider rates driver (1-5 stars)
- Driver rates rider (1-5 stars)
- Both can leave feedback

Storage:
- Rating aggregated per driver
- Average = sum of ratings / count
- Displayed to riders

Schema:
{
  "rideId": "ride_123",
  "raterType": "rider",
  "raterId": "rider_456",
  "rateeId": "driver_789",
  "rating": 4,
  "feedback": "Great driver, clean car",
  "timestamp": 1624050000
}
```

### Low Rating Handling
```
Driver rating drops below 4.5:
- Visible warning to drivers
- Less ride visibility
- May require training

Rating below 4.0:
- Rider/Driver may be deactivated
- Require improvement or appeals process

Fraud detection:
- Monitor rating patterns
- Detect systematic low ratings
- Investigate disputes
```

---

## Step 12: Scalability Considerations

### High Traffic Handling

```
During peak hours (10x normal):
- Auto-scale API servers
- Increase matching service instances
- Scale out database read replicas
- Increase Kafka partitions

Surge Pricing:
- Attracts more drivers
- Reduces rider demand
- Balances supply-demand

Request Queuing:
- Queue waiting riders
- Match as drivers become available
- Estimated wait time shown to user
```

### Database Sharding

```
Shard by City:
- San Francisco ? Shard 1
- New York ? Shard 2
- Los Angeles ? Shard 3
- London ? Shard 4

Each shard handles:
- Local matching
- Local drivers
- Local riders
- Regional surge pricing
```

---

## Step 13: Monitoring & Alerts

### Key Metrics
```
- Ride requests/second
- Average matching time
- Driver acceptance rate
- Rider cancellation rate
- Average rating
- System uptime
- API latency (p50, p95, p99)
- Database query latency
```

### Critical Alerts
```
- High request queue length (> 1000)
- No drivers available in region
- Matching latency > 1s
- Payment service down
- Database replication lag > 5s
- WebSocket connection failures
```

---

## Step 13: Key Design Decisions & Tradeoffs

| Decision | Why? | Tradeoff |
|----------|------|----------|
| Real-time location updates every 10-30 sec | Balance: frequent enough for accurate ETA, not so frequent to drain battery | Slightly stale location (10-30 sec old); more data points than necessary |
| Send request to top 3 drivers in parallel | Faster match; some drivers reject | Driver overload; 67% get rejected requests they didn't accept |
| Surge pricing during high demand | Incentivizes drivers to go online; balances supply/demand | Users pay more; social inequality (poor users priced out) |
| Eventual consistency for location | Fast updates; no coordination | Brief race conditions (driver appears in 2 cities) |
| Separate inventory for each city | Simplicity; can scale city independently | Complex multi-city trips; user moves city, data splits |

## Interview Cheat Sheet Q&A

**Q: Why not compute matching on the client (rider's phone)?**  
A: The rider's phone doesn't know where drivers are. That data is on the server. Also, rider might send matching request at 8pm, the server needs to decide which driver (driver availability changes second-by-second). Only the server has the global view.

**Q: What if a driver accepts a ride but goes offline during pickup?**  
A: Driver is marked BUSY, but location stops updating. After 5 minutes without location update, mark driver offline, reassign ride to another driver. Show rider: "Finding another driver..." Not ideal, but better than nothing.

**Q: Can we predict demand and pre-position drivers?**  
A: Yes! ML models predict: "At 5pm, downtown will have surge." Notify nearby drivers: "Surge predicted at 6pm, go downtown early." Incentivize with bonus. Reduces wait time during surge. However, predictions are imperfect; if you're wrong, drivers waste time.

**Q: Why not let riders book drivers in advance (like a taxi reservation)?**  
A: For airport pickups, maybe. But instant-ride is Uber's competitive advantage. Instant matching feels seamless. Advance booking adds friction (have to plan, commit to time). Rider wants: "Click, wait 5min, ride arrives."

**Q: What if two riders request at the exact same time and only 1 driver nearby?**  
A: Both see "Requesting... Estimated wait 8 min." Both are queued. When driver picks up rider A, rider B is auto-reassigned to the next available driver. Hopefully seconds later. Rider B might wait longer, but they're aware.

**Q: How do we prevent drivers from gaming surge pricing?**  
A: Monitor patterns. If driver X always comes online right before surge spike, flag as suspicious. Surge pricing is based on system demand, not individual behavior. Also, surge ends quickly when supply increases, so drivers can't sustain high prices for long.

 Summary

A ride-sharing system requires:
- ? Real-time location tracking and geospatial queries
- ? Efficient matching algorithm
- ? Dynamic surge pricing
- ? Accurate ETA calculation
- ? Scalable architecture for millions of users
- ? Real-time communication (WebSocket)
- ? Reliable payment processing
- ? Rating and feedback system
- ? Comprehensive monitoring




